# 運用ガイド（公開・更新・パスワード）

AWS資格学習ポータルを GitHub Pages で公開し、スマホでも閲覧できるようにするための運用手順です。

---

## 1. このサイトの構成

- 純粋な静的サイト（HTML / CSS / JS のみ。ビルド不要）。
- 各ページは `知見/<サービス名>.rtfd/*.jpg` の画像を直接参照しているため、**`知見` フォルダも一緒に公開する必要があります**。
- `js/auth.js` … 簡易パスワード画面（全ページの `<head>` で読み込み）。
- `.nojekyll` … GitHub Pages で日本語フォルダ名などをそのまま配信するための必須ファイル（消さない）。

---

## 2. 新しい知見を追加してサイトに反映する手順

1. **素材を置く**：`知見` フォルダに新しい `.rtfd`（テキスト＋スクショ）を追加する。
2. **ページ化を依頼**：Claude に「`知見` の○○をページ化して」と頼む。Claude が次を行います。
   - `services/<id>.html`（または `concepts/<id>.html`）を既存ページと同じ体裁で作成
   - `js/app.js` の `NAV_GROUPS` に項目を追加（`enabled: true`）
   - 画像は `../知見/<サービス名>.rtfd/<画像>.jpg` を参照
3. **公開**：以下を実行（数十秒で本番反映）。
   ```bash
   git add -A
   git commit -m "add: ○○ページ"
   git push
   ```
4. スマホで Pages の URL を再読み込みすると反映されています。

---

## 3. 初回だけ：GitHub Pages への公開セットアップ

> `gh`（GitHub CLI）が未インストールの場合は、ブラウザでリポジトリを作る方法（3-B）でも可。

### 共通：git 初期化
```bash
cd "/Users/kazuya_idemoto/Library/CloudStorage/OneDrive-個人用/Claude/資格学習/AWS"
git init
git config user.name  "あなたの名前"
git config user.email "あなたのメール"
git add -A
git commit -m "initial: AWS資格学習ポータル"
```

### 3-A. GitHub CLI を使う場合
```bash
brew install gh          # 未インストールなら
gh auth login            # 画面の指示に従ってログイン
gh repo create aws-study-portal --public --source=. --push
# Pages を有効化（main ブランチのルートを公開）
gh api -X POST repos/{owner}/aws-study-portal/pages -f 'source[branch]=main' -f 'source[path]=/'
```

### 3-B. ブラウザでリポジトリを作る場合
1. GitHub で新規リポジトリ（例 `aws-study-portal`、**Public**）を作成。
2. 表示された手順に従ってリモート追加 & push：
   ```bash
   git remote add origin https://github.com/<ユーザー名>/aws-study-portal.git
   git branch -M main
   git push -u origin main
   ```
3. リポジトリの **Settings → Pages** で「Source: Deploy from a branch」「Branch: main / (root)」を選択して保存。

### 公開 URL
```
https://<ユーザー名>.github.io/aws-study-portal/
```
このURLをスマホで開く → パスワードを入力 → 閲覧開始。

---

## 4. パスワードの変更方法

初期パスワードは **`aws-study`** です。**必ず自分のパスワードに変更してください。**

1. 新パスワードの SHA-256 を生成：
   ```bash
   printf '%s' 'あたらしいパスワード' | shasum -a 256 | awk '{print $1}'
   ```
2. 出力された64桁の文字列を `js/auth.js` の `PASSWORD_HASH` に貼り替える。
3. 公開：
   ```bash
   git add -A && git commit -m "change password" && git push
   ```
4. 既にログイン済みの端末は影響を受けません（再ログインさせたい場合は各端末でブラウザのサイトデータを消す）。

---

## 5. スマホでのチャット（AI質問）について

- チャットは各端末で**初回に自分の Gemini APIキー**を入力します（端末ごとに `localStorage` 保存）。
- キーは https://aistudio.google.com/apikey から無料で取得できます。
- 共有端末では、チャットの ⚙️ から「削除」しておくと安全です。

---

## 6. 注意（セキュリティ）

- **GitHub 無料枠の Pages は公開リポジトリが前提**です。そのため**リポジトリのソース（`知見` の原資料・スクショ・`auth.js` のハッシュ）は GitHub 上で誰でも閲覧可能**になります。パスワード画面は「Pages サイト」のみを守り、「GitHub リポジトリ」は守りません。
- 原資料も非公開にしたい場合は、無料で**非公開ソース＋本格的なパスワード（メール認証など）**にできる **Cloudflare Pages + Cloudflare Access** への切り替えが有効です（必要なら対応します）。
- `js/auth.js` の簡易パスワードは、技術的には突破され得るカジュアルな保護です。
