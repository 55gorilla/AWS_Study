# 運用ガイド（公開・更新・アクセス管理）

AWS資格学習ポータルは **Cloudflare Workers（静的配信）＋ Cloudflare Access（メールOTPログイン）** で公開しています。
スマホでもPCでも、メールに届くワンタイムコードでログインして閲覧できます。

---

## 0. 公開URL

```
https://aws-study.docomo-au-softbank0815.workers.dev
```

- アクセスすると Cloudflare のログイン画面 → メールアドレス入力 → 届いたコードを入力 → 閲覧。
- **パスワードはありません**（毎回メールにコードが届く方式なので「覚える・変更する・忘れる」がそもそも不要）。

---

## 1. このサイトの構成

- 純粋な静的サイト（HTML / CSS / JS のみ。ビルド不要）。
- 各ページは `知見/<サービス名>.rtfd/*.jpg` の画像を直接参照（`知見` フォルダも一緒に配信）。
- `wrangler.jsonc` … Cloudflare Workers の配信設定（`assets.directory = "."` でルート全体を配信）。
- GitHub リポジトリ：`55gorilla/AWS_Study`（ソース管理）。
- 配信：**GitHub に push すると Cloudflare が自動デプロイ**。

---

## 2. 新しい知見を追加してサイトに反映する手順

1. **素材を置く**：`知見` フォルダに新しい `.rtfd`（テキスト＋スクショ）を追加する。
2. **ページ化を依頼**：Claude に「`知見` の○○をページ化して」と頼む。Claude が次を行います。
   - `services/<id>.html`（または `concepts/<id>.html`）を既存ページと同じ体裁で作成
   - `js/app.js` の `NAV_GROUPS` に項目を追加（`enabled: true`）
   - 画像は `../知見/<サービス名>.rtfd/<画像>.jpg` を参照
3. **公開**：以下を実行（push で Cloudflare が自動デプロイ → 1〜2分で反映）。
   ```bash
   git add -A
   git commit -m "add: ○○ページ"
   git push
   ```
4. スマホ／PCでログイン済みなら、再読み込みで反映されています。

---

## 3. アクセスできる人を管理する（許可メールの追加・削除）

ログインできる人は Cloudflare Access のポリシーで決まります。

1. https://one.dash.cloudflare.com を開く（Cloudflareアカウント）
2. **「Access」→「Applications」→ `aws-study...workers.dev`** を選択
3. **「Policies（ポリシー）」** を開く
4. **Include（含める）→ Emails** に、許可したい人のメールを追加／削除して保存
   - ⚠️ 必ず特定メールのみ許可（空や「Everyone」にしない）。
   - 家族や同僚に見せたいときは、その人のメールを追加すれば、その人もメールOTPでログインできます。

---

## 4. ログインの有効期間を変えたい場合（任意）

- Access の Application 設定で **Session Duration（セッション期間）** を変更できます（例：24時間／1週間／1か月）。
- 長くするとログインの手間は減りますが、共有端末では短めが安全です。

---

## 5. スマホでのチャット（AI質問）について

- チャットは各端末で**初回に自分の Gemini APIキー**を入力します（端末ごとに `localStorage` 保存）。
- キーは https://aistudio.google.com/apikey から無料で取得できます。
- 共有端末では、チャットの ⚙️ から「削除」しておくと安全です。

---

## 6. メモ（旧方式からの変更点）

- 以前の「JS簡易パスワード（`js/auth.js`）」は **撤去済み**。認証は Cloudflare Access に一本化。
- 「GitHub Pages」での公開は **無効化済み**（`github.io` 側からは閲覧不可）。公開は Cloudflare のみ。
- リポジトリは Public のため、**ソース（`知見` の原資料・スクショ）は GitHub 上では閲覧可能**な点は従来どおり。原資料も完全非公開にしたい場合は、リポジトリを Private にして Cloudflare の「直接アップロード（Wrangler）」デプロイへ切り替える方法があります（必要なら対応します）。
